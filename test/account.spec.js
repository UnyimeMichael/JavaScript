const {Account} = require("../intermediate.js/class.js")

test("Account class is not null", ()=> {
    let newAccount = new Account ("Mariam klien", "mk@gmail", "87652743538")
    expect(newAccount).not.toBeNull();
});
test("should generate account number", () =>)



